load("@rules_python//python/uv:lock.bzl", "lock")

lock(
    name = "requirements",
    srcs = ["pyproject.toml"],
    out = "requirements.txt",
)

lock(
    name = "requirements_dev",
    srcs = ["pyproject.toml"],
    out = "requirements_dev.txt",
    args = [
        "--extra",
        "dev",
    ],
)
